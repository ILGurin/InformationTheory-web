# InformationTheory-web
It is a web application that allows you to count entropy, and encode words using offset and permutation methods


## Installation

Install dependencies:

```shell
npm install
```

Run the server:
```shell
npm run dev
```